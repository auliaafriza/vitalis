{
  "name": "@calorya/api",
  "version": "0.1.0",
  "private": true,
  "description": "Typed Supabase data layer. Pure functions over a SupabaseClient \u2014 each app supplies its own client (cookies on web, AsyncStorage on mobile).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "@calorya/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
